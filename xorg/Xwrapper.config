# Allow the displaycameras systemd service to start Xorg from the console
# (there is no logind graphical seat when launched this way).
# Installed to /etc/X11/Xwrapper.config
allowed_users=anybody
needs_root_rights=yes
